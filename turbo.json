{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "preview": {
      "cache": false,
      "dependsOn": ["build"]
    },
    "lint": {
      "outputs": []
    },
    "test": {
      "inputs": ["**/*.tsx", "**/*.ts"],
      "outputs": []
    },
    "test:coverage": {
      "inputs": ["**/*.tsx", "**/*.ts"],
      "outputs": ["coverage/**"]
    },
    "typecheck": {
      "inputs": ["**/*.tsx", "**/*.ts"],
      "outputs": []
    }
  }
}
